# Same CLIENT_ID as in oauth/.env
VITE_GITHUB_CLIENT_ID=YOUR-GITHUB-OAUTH-APP-ID
# URL of the oauth/ backend server
VITE_BACKEND_URL=http://localhost:3000
# Must match the callback URL registered in your GitHub OAuth App
VITE_GITHUB_REDIRECT_URI=http://localhost:5173
